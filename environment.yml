# conda env create --file environment.yml
name: vinos_coderhouse
channels:
  - anaconda
  - conda-forge
  - defaults
dependencies:
  - pandas-profiling=3.2.0
  - fs
  - jupyterlab
  - jupyter
  - pathlib
  - pip
  - pyprojroot
  - python=3.9
  - pip:
    - pyhere
    